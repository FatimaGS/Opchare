<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="@drawable/fondoprincipal1app"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity"
    android:orientation="vertical"
    android:gravity="center" >
    
    <Button
        android:id="@+id/button3"
        android:drawableLeft="@drawable/camaraapp"
        android:layout_width="fill_parent"
        android:layout_height="110dp"
        android:layout_centerHorizontal="true"
        android:textColor="#ffffff"
        android:gravity="center"
        android:onClick="GuardarActivity"
        android:padding="10dp"
        android:paddingLeft="40dp"
        android:text="@string/Guardar"
        android:background="@drawable/pulsacionboton"
        android:textSize="40sp"
        android:textStyle="bold" />

    <Button
        android:id="@+id/button2"
        android:drawableLeft="@drawable/galeriaapp"
        android:layout_width="fill_parent"
        android:layout_height="110dp"
        android:layout_centerHorizontal="true"
        android:textColor="#ffffff"
        android:layout_below="@+id/button3"
        android:layout_marginTop="50dp"
        android:gravity="center"
        android:onClick="SacarActivity"
        android:padding="10dp"
        android:paddingLeft="40dp"
        android:text="@string/Sacar"
        android:background="@drawable/pulsacionboton"
        android:textSize="40sp"
        android:textStyle="bold"/>
    
</RelativeLayout>